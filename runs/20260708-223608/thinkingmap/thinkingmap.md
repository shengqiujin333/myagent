# 🧠 Project Concept Corpus & Thinking Map

## 1. 核心业务抽象 (Core Business Abstraction)
交流注入内阻测量，STM32G030状态机固件，PWM→恒流源→ADC同步采样，DS18B20温度，UART级联主从自定义协议，多点校准/相位补偿/异常剔除，无阻塞前后台调度

## 2. 增量聚类迭代追踪 (Incremental Clustering Iteration Trace)

### 🔄 Round 1
- **新生成词数**: ~50个
- **新涌现类别**: 
  - 物理层与电气接口
  - 信号调理与采样链
  - 时序与同步机制
  - UART级联通讯协议基础
  - 状态机与固件架构
- **饱和度状态**: 未饱和，继续迭代

### 🔄 Round 2
- **新生成词数**: ~50个
- **新涌现类别**:
  - 校准与补偿算法
  - 异常检测与数据滤波
  - 温度传感器接口 (DS18B20)
  - 电源与电压基准
  - 复位与监视 (看门狗/掉电)
- **饱和度状态**: 未饱和，继续迭代

### 🔄 Round 3
- **新生成词数**: ~50个
- **新涌现类别**:
  - 失效模式与可靠性
  - 量产测试与标定
  - 电磁兼容与保护
  - 生命周期与文档规范
- **饱和度状态**: 未饱和，继续迭代

### 🔄 Round 4
- **新生成词数**: ~50个
- **新涌现类别**: **无 (0 个)**。所有词均归入已有类别。
- **饱和度状态**: **【已饱和 (Saturated)】，概念维度探索完毕，终止循环。**

---

## 3. 最终多维概念语料库 (Final Multi-dimensional Concept Corpus)

### 物理层与电气接口
地弹效应，回流路径，星型接地，模拟地分割，数字地分割，单点接地，Kelvin四线连接，开尔文接触，接触电阻，热电动势，塞贝克效应，焊点疲劳，导线电阻，PCB寄生参数，走线电感，互容，串扰，反射，振铃，过冲，下冲，特性阻抗，终端匹配，串联电阻，RC吸收，磁珠，共模扼流圈，隔离栅，光耦隔离，电容隔离，磁隔离，CA-IS3721，隔离耐压，爬电距离，电气间隙，CTI，漏电起痕，静电放电，ESD，TVS，压敏电阻，气体放电管，放电间隙，滤波电容，去耦电容，ESR，ESL，自谐振频率，电源纹波，PSRR，线路调率，负载调率，瞬态响应，恢复时间，输出电压噪声，LDO，XC6206，压差，Dropout，效率，静态电流，使能引脚，软启动，限流，热关断，温度保护，SOT-23，封装热阻，散热焊盘，热过孔，导热率，功率耗散，结温，Tjmax，降额设计

### 信号调理与采样链
PWM生成，TIM3_CH3，1kHz方波，占空比50%，RC滤波网络，四阶RC，正弦波生成，电压跟随器，运放恒流源，Howland电流源，改进型Howland，Wilson恒流源，交流注入，激励电流幅值，失真，THD，噪声密度，运放选型，LMV358，LM258，轨到轨，GBW，压摆率，建立时间，失调电压，温漂，偏置电流，输入阻抗，共模抑制比，CMRR，电源抑制比，PSRR，开环增益，相位裕度，稳定性，反馈网络，补偿电容，放大器噪声，闪烁噪声，白噪声，总噪声RMS，信号调理，抗混叠滤波器，低通滤波器，巴特沃斯特，切比雪夫，椭圆函数，Sallen-Key，多反馈，MFB，RLC谐振，品质因数Q，截止频率，阻带衰减，滚降，通带纹波，群延迟，相位响应，相移，延迟，孔径抖动，采样保持，ADC转换，STM32G030内部ADC，12位分辨率，逐次逼近SAR，采样时间，转换时间，规则通道，注入通道，扫描模式，单次转换，连续转换，触发源，定时器触发，软件触发，DMA请求，结果对齐，右对齐，左对齐，过采样，抽取，噪声整形，有效位数ENOB，INL，DNL，偏移误差，增益误差，原始码，量化误差，采样率10kHz，同步采样，相位同步，零交叉点，过零检测，锁相环，PLL，数字锁相环，软件PLL，相关检测，相敏检波，DSP，数字相敏检测

### 时序与同步机制
系统时钟64MHz，HSE 8MHz，PLL倍频，分频，时钟树，时钟安全系统CSS，时钟切换，HSI，LSE，LSI，RTC，系统节拍，Systick，软件定时器，硬件定时器，TIM3，TIM14，TIM1，PWM模式，输出比较，输入捕获，单脉冲模式，门控模式，定时器级联，同步，触发，更新事件，影子寄存器，预装载，缓冲，自动重载，向上计数，向下计数，中心对齐，边沿对齐，死区时间，刹车输入，互补输出，同步信号，帧同步，相位对齐，ADC与PWM同步，注入触发，规则触发，DMA传输完成，中断，中断优先级，嵌套，抢占优先级，子优先级，BASE优先，中断延迟，中断响应，中断返回，上下文保存，上下文恢复，堆栈，MSP，PSP，双堆栈，异常，HardFault，NMI，SysTick，PendSV，SVCall，SVC，任务切换，上下文切换，临界区，关中断，互锁，原子操作，指令重排，内存屏障，时序约束，最坏情况执行时间WCET，抖动，相位噪声，频率稳定度，漂移，时钟偏移，时钟抖动，建立时间，保持时间，同步器，两级同步，跨时钟域，异步FIFO

### UART级联通讯协议基础
UART1主机模式，UART2从机模式，TX，RX，串行数据帧，起始位，数据位，奇偶校验，停止位，波特率，115200，位定时，过采样，收发器，RS-232电平，UART接口，硬件流控，RTS，CTS，软件流控，XON，XOFF，中断接收，轮询发送，DMA发送接收，环形缓冲区，FIFO，半满中断，空闲中断，超时检测，帧错误，溢出错误，奇偶错误，噪音检测，BREAK，线路断开，自定义协议，帧格式，帧头，帧尾，长度域，命令域，地址域，数据域，校验域，CRC，CRC-8，多项式，初始值，异或，校验和，累加和，重复帧，重排序，超时重传，ACK，NAK，序列号，窗口，流控，背压，拥塞，轮询机制，广播地址，单播地址，组播，地址分配，动态地址，冲突检测，载波侦听，主从握手，心跳包，同步帧，应答超时，节点发现，邻居发现，拓扑发现，菊花链，手拉手，电平匹配，M_S引脚，上拉输入，主机从机判断，内部上拉，外部上拉100kΩ，电平识别，开机检测

### 状态机与固件架构
有限状态机，Moore型，Mealy型，状态迁移图，状态表，状态编码，二进制编码，格雷码，独热码，状态冗余，非法状态，状态恢复，错误状态，默认状态，事件驱动，事件枚举，动作表，转移条件，case switch，函数指针，表驱动，嵌套状态机，层次状态机，HFSM，并发状态，子状态，状态切换开销，无阻塞，非阻塞，轮询，超循环，调度器，时间片轮转，任务表，前后台系统，ISR，主循环，中断服务函数，任务优先级，临界区保护，信号量，邮箱，队列，事件标志，消息传递，状态机嵌套，深度，递归，栈深度，堆栈溢出检测，看门狗喂狗，独立看门狗IWDG，窗口看门狗WWDG，喂狗时机，复位，NRST，外部复位，内部复位，软复位，硬件复位，复位源，低电压检测BOR，POR，PVD，电源监视，掉电检测，数据保存，EEPROM模拟，Flash写入，擦除，磨损均衡，备份，校验，恢复，降级模式，安全状态，故障保，反走，看门狗溢出

### 校准与补偿算法
多点校准，校准曲线，分段线性插值，多项式拟合，最小二乘法，加权最小二乘法，鲁棒回归，M估计，RANSAC，校准点，标定，参考电阻，标准源，基准值，偏移校正，增益校正，线性度校正，残差，拟合误差，残差平方和，决定系数R²，插值方法，线性插值，拉格朗日插值，样条插值，查找表，LUT，表索引，量化，分度，校准系数，卡尔曼滤波，状态估计，递推最小二乘，RLS，遗忘因子，自适应滤波，相位补偿，相移校正，群延迟补偿，全通滤波器，希尔伯特变换，正交变换，IQ解调，复数运算，幅度相位提取，鉴相，鉴幅，数字下变频，DDC，混频，低通滤波，I/Q分量，反正切，CORDIC，坐标旋转数字计算机

### 异常检测与数据滤波
异常值，离群点，粗大误差，野点，脉冲干扰，充电干扰，放电干扰，充电电流纹波，放电电流峰值，负载突变，瞬态过程，鉴别准则，3σ准则，Grubbs检验，Dixon检验，Chauvenet准则，Tukey fence，箱线图，四分位距，IQR，中位数绝对偏差MAD，基于残差的检测，滑动窗口，指数加权移动平均EWMA，累计和CUSUM，变化点检测，阈值，滞回比较器，施密特触发器，毛刺，尖峰，边沿检测，去抖，软件滤波，中值滤波，均值滤波，滑动平均，加权平均，FIR滤波，IIR滤波，低通滤波，高通滤波，带通滤波，带阻滤波，巴特沃斯，切比雪夫，椭圆，零相位滤波，双向滤波，Kalman滤波，粒子滤波，数字信号处理CMSIS-DSP，arm_fir_f32，arm_iir_f32，arm_fir_decimate，arm_fir_interpolate，定点化，Q格式，标度，饱和，限幅，溢出保护

### 温度传感器接口 (DS18B20)
DS18B20，单总线协议，1-Wire，DQ引脚，上拉电阻4.7kΩ，寄生供电，强上拉，MOSFET驱动，时序要求，初始化脉冲，存在脉冲，ROM指令，功能指令，跳过ROM，匹配ROM，搜索ROM，温度转换，读取暂存器，写入暂存器，复制暂存器，召回EE，读取电源模式，CRC校验，CRC8，循环冗余校验，多项式，查表法，位操作，精确时序，延时函数，微秒级延时，定时器捕获，微秒中断，DWT周期计数，SysTick微秒，阻塞与非阻塞，状态机实现，温度测量序列，转换时间，12位分辨率，9位，10位，11位，温度精度，误差，校准，温度系数，温漂，热响应时间

### 电源与电压基准
电源架构，+12V输入，LDO稳压3.3V，XC6206P332，隔离电源，VPS8701B，变压器VPT87DFB01B，隔离后3.3VA，5VA，1.65V参考电压，TLV431A，可调并联稳压器，分压电阻，R3=660Ω，R4=820Ω，基准输出精度，温度系数，噪声，长期稳定性，电源域，隔离前，隔离后，共地，ISOGND，模拟电源，数字电源，去耦，磁珠隔离，π型滤波，LC滤波，电源完整性，PI，阻抗，目标阻抗，去耦电容，ESR，ESL，自谐振频率，电流能力，效率，热耗散，电源瞬态响应，负载瞬态，纹波抑制，PSRR

### 复位与监视
NRST引脚，外部复位电路，RC复位，手动复位，复位时间，复位释放，上电复位POR，掉电复位BOR，可编程电压检测器PVD，阈值电压，VDD监测，中断，复位，电源掉电，数据保存，Flash擦写保护，选项字节，读保护，写保护，看门狗，独立看门狗IWDG，LSI，时钟源，分频，重装载值，喂狗，写保护，窗口看门狗WWDG，窗口上限，窗口下限，早喂狗，晚喂狗，复位，硬件狗，软件狗，看门狗复位标志，复位源识别，系统复位，内核复位，外部复位，看门狗复位，低功耗管理，睡眠模式，停止模式，待机模式，唤醒，唤醒引脚，RTC唤醒，EXTI唤醒，UART唤醒，ADC唤醒，定时器唤醒，低功耗串口，LPUART，功耗优化，动态功耗，静态功耗，漏电流，时钟门控，外设时钟使能，自动低功耗模式

### 失效模式与可靠性
失效模式，开路，短路，漂移，老化，温漂，焊点疲劳，虚焊，冷焊，锡须，电迁移，应力迁移，热应力，机械振动，冲击，跌落，温度循环，湿度，盐雾，霉菌，沙尘，低气压，加速寿命测试，HALT，HASS，烧机，老炼，环境试验，EMC试验，ESD试验，EFT，浪涌，脉冲群，辐射抗扰度，传导抗扰度，辐射发射，传导发射，静电放电，系统级ESD，人体模型，HBM，充电器件模型，CDM，机器模型，MM，闩锁效应，Latch-up，过流，短路，过热，过压，欠压，反接保护，反向电流，电源极性，误接线，管脚短路，IO容忍，未用管脚处理，浮空输入，上拉/下拉，状态未知，初始化顺序，上电时序，下电时序，信号竞争，冒险，毛刺，亚稳态，跨时钟域故障，空指针，野指针，栈溢出，堆溢出，数组越界，整数溢出，除零，无穷递归，死循环，看门狗超时，复位，故障注入，故障注入测试，FIT，失效率，MTBF，MTTF，可靠性预计，FMEA，DFMEA，PFMEA，故障树FTA，根本原因分析RCA，纠正措施，预防措施，降额设计，容错，冗余，备份，看门狗恢复，状态恢复，错误日志，故障记录，在线诊断，自检，BIT，开机自检，周期性自检，报错代码，指示灯

### 量产测试与标定
功能测试FCT，在线测试ICT，飞针测试，边界扫描，JTAG，测试点，焊盘，探针，针床，夹具，良率，缺陷，短路，开路，焊接质量，BOM核对，元器件极性，方向，贴装，回流焊，波峰焊，手工焊，清洗，助焊剂，绝缘电阻，耐压测试，开机测试，固件烧录，离线烧录，在线烧录，SWD，pyOCD，Keil，批量烧录，序列号，MAC，校准，标定，标准源，参考仪表，校准曲线，校准系数，储存，校验，温度补偿，产线自动化，测试脚本，自学习，误测，复测，统计过程控制SPC，控制图，Cp，Cpk，合格率，报废率，返修率，测试覆盖，测试用例，冒烟测试，老化测试，筛选

### 电磁兼容与保护
电磁兼容EMC，辐射发射RE，传导发射CE，辐射抗扰度RS，传导抗扰度CS，静电放电ESD，电快速瞬变脉冲群EFT，浪涌SURGE，电压跌落，中断，谐波，闪烁，共模干扰，差模干扰，共模电压，共模抑制，屏蔽，接地，浮地，等电位，滤波，共模扼流圈，差模扼流圈，铁氧体磁环，磁珠，电容滤波，LC滤波，π型滤波，T型滤波，TVS管，压敏电阻，气体放电管，瞬态抑制，放电间隙，光耦隔离，电磁隔离，电容隔离，布局，布线，分区，隔离带，地平面，缝隙，跨接，电感，变压器屏蔽，线缆屏蔽，双绞线，屏蔽层接地，360°端接，搭接，连接器，滤波连接器，ESD防护器件，钳位二极管，齐纳二极管，RC缓冲，吸收，谐振，阻尼，辐射耦合，传导耦合，感性耦合，容性耦合，共阻抗耦合，回路面积，最小回路，电流环路，天线效应，共模辐射，差模辐射，频率，波长，四分之一波长，天线，近场，远场，屏蔽效能，转移阻抗，插入损耗，滤波特性，频率响应，插入损耗，反射损耗，吸收损耗

### 生命周期与文档规范
需求规格，系统设计，详细设计，测试计划，测试报告，用户手册，维护文档，版本控制，Git，分支，标签，提交，变更管理，ECN，ECO，配置管理，BOM，物料编码，替代料，第二货源，生命周期，停产，EOL，MIL，退役，更新，升级，固件版本，硬件版本，兼容性，向后兼容，向前兼容，部署，安装，配置，重装，恢复，备份，归档，合规，CE认证，FCC，UL，RoHS，REACH，WEEE，EMC指令，LVD指令，安全标准，IEC 61000，IEC 60950，IEC 62368，ISO 9001，ISO 13485，TS 16949，IATF 16949，AEC-Q100，车规，工业级，商业级，扩展温度，温度范围，气候类别，防护等级，IP等级，防尘，防水，跌落，冲击，振动，运输，包装，防静电，ESD防护，湿度敏感，MSL，烘烤，真空包装，操作规范，SOP，培训，技能，知识库，问题跟踪，JIRA，bug，缺陷，严重性，优先级，复现，解决，验证，关闭，缺陷管理，根本原因分析，纠正措施，预防措施，持续改进，PDCA，Kaizen，精益生产，六西格玛，DMADV，DFSS，质量门，里程碑，评审，设计评审，代码评审，测试评审，验收评审