# Slice: T-2.1

## Role

Embedded firmware engineer focused on finishing one directly executable task.

## Objective

配置TIM3_CH3产生1kHz 50%占空比方波；使用TIM3更新事件或输出比较触发ADC同步采样（同时配置ADC1三通道注入组或规则组实现10kHz采样率）。设计相位延迟修正机制：通过软件延时或TIM3计数器预加载值调整ADC采样窗口，补偿RC滤波引入的相移。

## Target Files

- Not specified by plan

## Acceptance

- build: configured default
- flash: configured default
- serial: 输出可测量到1kHz方波；ADC在方波上升沿触发采样（或固定相位偏移），采样率稳定在10kHz±5%；相位调整可通过宏定义改变。

## Protocol

- Do not create child tasks.
- Keep model messages compact; read artifact files by path.
- Integrate code into the configured main entrypoint.
- Verify by build, CMSIS-DAP flash, and serial or host-test observation.

## Context Files

- `runs\20260709-090019\context\context.md`
- `runs\20260709-090019\thinkingmap\thinkingmap.md`
