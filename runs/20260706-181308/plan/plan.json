{
  "tasks": [
    {
      "id": "integrate-smoke-check",
      "title": "Add firmware smoke check integrated into main",
      "kind": "code",
      "objective": "Add a complete firmware smoke check module wired into main.",
      "dependencies": [],
      "target_files": [
        "src/main.c"
      ],
      "acceptance": [
        {
          "kind": "build",
          "command": "cmake --build build",
          "expected": null
        },
        {
          "kind": "flash",
          "command": "pyocd flash build/firmware.elf",
          "expected": null
        },
        {
          "kind": "serial",
          "command": null,
          "expected": "SMOKE OK"
        }
      ]
    }
  ]
}